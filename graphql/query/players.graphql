query Players {
  players {
    id
    name
  }
}

query Player($id: ID!) {
  players {
    id
    name
    hand {
      suit,
      value,
      code,
      image
    }
    role
    bet
    wallet
    hasTurn
  }
}

